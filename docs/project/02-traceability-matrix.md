# Requirements Traceability Matrix (RTM) — Baseline B1

**Chain:** Requirement → Milestone → Epic → Feature → User Story → Task → Code → Test → PR → Release

This matrix is the project's contractual record that every requirement is accounted for, and that
nothing is built without a requirement. **It is a living document** — the Task, Code, Test, PR and
Release columns are populated as work completes, and no PR may merge without updating its row.

**Baseline:** B1 — 2026-07-19
**Scope:** 28 SRS functional requirements · 8 NFRs · 3 constraints
**Backlog:** 4 phases · 19 epics · 93 features · **178 user stories** · **524 tasks** · **743 story points**

**Status key**

- ✅ **Ready** — every story authorized and unblocked
- 🟠 **Sim-only** — buildable now against the ACS simulator, but not *verifiable* until TODO-02
- 🟡 **Partial (n/m)** — n of m stories are workable; the rest are blocked
- 🔴 **Blocked** — every story blocked

A requirement is **Blocked** only when *all* of its stories are. Most are **Partial**: the core
behaviour is buildable while a dependent slice — usually master data (TODO-01) — waits.

---

## Forward trace — Requirement → delivery

| Requirement | Phase | Epic(s) | Stories | Status | Gating |
|---|---|---|---|---|---|
| **FR-VMS-01** | 2 | EPIC-07 | 7 | ✅ Ready | — |
| **FR-VMS-02** | 2, 4 | EPIC-07, EPIC-17 | 7 | 🟡 Partial (6/7) | TODO-01 |
| **FR-VMS-03** | 2 | EPIC-08 | 5 | ✅ Ready | — |
| **FR-VMS-04** | 2, 3 | EPIC-08, EPIC-12 | 4 | ✅ Ready | — |
| **FR-VMS-05** | 2 | EPIC-09 | 4 | 🟠 Sim-only | TODO-02 |
| **FR-VMS-06** | 2 | EPIC-09, EPIC-10 | 4 | 🟡 Partial (2/4) | TODO-01, TODO-02 |
| **FR-VMS-07** | 2 | EPIC-09 | 5 | 🟡 Partial (4/5) | TODO-02, TODO-17 |
| **FR-VMS-08** | 3 | EPIC-12, EPIC-13 | 5 | 🟡 Partial (2/5) | TODO-12, TODO-13, TODO-18 |
| **FR-VMS-09** | 3 | EPIC-13 | 2 | 🟡 Partial (1/2) | TODO-07 |
| **FR-VMS-10** | 3 | EPIC-13 | 2 | 🔴 Blocked | TODO-02, TODO-03, TODO-19 |
| **FR-VMS-11** | 1, 2, 4 | EPIC-04, EPIC-08, EPIC-09, EPIC-17 | 7 | 🟡 Partial (5/7) | TODO-01, TODO-02 |
| **FR-VMS-12** | 3 | EPIC-14 | 2 | 🔴 Blocked | TODO-02, TODO-09 |
| **FR-VMS-13** | 1, 2 | EPIC-04, EPIC-09 | 3 | 🟡 Partial (2/3) | TODO-01, TODO-02 |
| **FR-VMS-14** | 3 | EPIC-14 | 2 | 🟡 Partial (1/2) | TODO-07 |
| **FR-VMS-15** | 2, 3 | EPIC-09, EPIC-12 | 3 | 🔴 Blocked | TODO-02, TODO-17 |
| **FR-CRD-01** | 3 | EPIC-15 | 2 | ✅ Ready | — |
| **FR-CRD-02** | 3 | EPIC-15 | 2 | 🔴 Blocked | TODO-02, TODO-11 |
| **FR-CRD-03** | 3, 4 | EPIC-12, EPIC-15, EPIC-17 | 8 | 🟡 Partial (1/8) | TODO-01, TODO-02, TODO-10, TODO-11 |
| **FR-NOT-01** | 2, 4 | EPIC-09, EPIC-16, EPIC-17 | 9 | 🟡 Partial (7/9) | TODO-02, TODO-05, TODO-10 |
| **FR-NOT-02** | 3, 4 | EPIC-11, EPIC-12, EPIC-17 | 4 | 🟡 Partial (3/4) | TODO-01, TODO-02 |
| **FR-REP-01** | 3, 4 | EPIC-11, EPIC-13, EPIC-18 | 8 | 🟡 Partial (6/8) | TODO-02, TODO-03, TODO-12, TODO-13 |
| **FR-REP-02** | 3, 4 | EPIC-15, EPIC-18 | 2 | 🔴 Blocked | TODO-11 |
| **FR-API-01** | 2, 3, 4 | EPIC-09, EPIC-11, EPIC-13, EPIC-17 | 10 | 🟡 Partial (7/10) | TODO-01, TODO-02, TODO-07 |
| **FR-API-02** | 1, 3 | EPIC-05, EPIC-11, EPIC-14 | 4 | 🟡 Partial (2/4) | TODO-01, TODO-12 |
| **FR-API-03** | 1, 3 | EPIC-05, EPIC-11 | 4 | 🟡 Partial (1/4) | TODO-02, TODO-12 |
| **FR-ADM-01** | 1 | EPIC-02, EPIC-04 | 2 | 🟡 Partial (1/2) | TODO-01 |
| **FR-ADM-02** | 1 | EPIC-02, EPIC-04, EPIC-06 | 7 | 🟡 Partial (2/7) | TODO-01, TODO-08, TODO-15 |
| **FR-ADM-03** | 1, 4 | EPIC-06, EPIC-19 | 3 | 🟡 Partial (1/3) | TODO-01, TODO-16 |

**Coverage: 28 / 28 SRS functional requirements decomposed to user story level (100%).**
Full story and task detail lives in [`backlog/`](backlog/); this table is the index into it.

---

## Non-functional requirement trace

| Requirement | Phase | Epic(s) | Verification method | Status |
|---|---|---|---|---|
| NFR-PRF-01 | 3 | EPIC-11 | Performance test at the reception UI | 🔴 **Untestable — no target (TODO-07)** |
| NFR-REL-01 | 3 | EPIC-11 | Chaos test: ACS unavailable during issuance | ✅ Ready |
| NFR-AVL-01 | 1, 3 | EPIC-01, EPIC-11 | Uptime monitoring; ACS-independence test | 🟡 Operating hours undefined |
| NFR-SEC-01 | 1 | EPIC-02, EPIC-03, EPIC-05 | OWASP ASVS review + RBAC integration tests | ✅ Ready |
| NFR-SCL-01 | 1 | EPIC-01 | Load test: 500 visitors/day, 120 concurrent users | ✅ Ready |
| NFR-USA-01 | 1 | EPIC-06 | Usability review with reception staff | 🟡 No acceptance criteria; WCAG target is derived |
| NFR-MNT-01 | 1, 3 | EPIC-01, EPIC-11 | **Architecture fitness test — no ACS type outside the integration module** | ✅ Ready |
| NFR-CMP-01 | 1 | EPIC-05 | Data protection impact assessment | 🔴 Blocked — TODO-06, TODO-12 |

| Constraint | Enforced by | Verification |
|---|---|---|
| CON-01 | EPIC-11 | Fitness test — no hardware-specific type anywhere in the codebase |
| CON-02 | EPIC-11 (F-11.1) | Fitness test — no credential mutation path bypasses the ACS port |
| CON-03 | EPIC-06 (F-06.1) | Design review against client brand guidelines |

---

## Reverse trace — is anything being built without a requirement?

Run at **every sprint review**. Any row that cannot name a requirement is either an approved enabler
or is closed as out of scope. This is the control that makes project rule 2 real rather than
aspirational.

### Measured provenance across all 178 user stories

| Provenance | Stories | Share | Implementable? |
|---|---|---|---|
| `SRS` | 67 | 38% | ✅ Yes |
| `SRS-NFR` | 19 | 11% | ✅ Yes |
| `SRS-CON` | 3 | 2% | ✅ Yes |
| `ENABLER` | 16 | 9% | ✅ Yes — justified against an NFR |
| `TDD-DERIVED` | 62 | 35% | ⚠️ **No — backlog only, pending TODO-01** |
| `BLOCKED` | 11 | 6% | ⛔ No |

**105 stories (59%) are authorized. 73 (41%) are not.**

### Concentration by phase

| Phase | Stories | TDD-derived | Blocked | Authorized |
|---|---|---|---|---|
| 1 — Platform Foundation | 42 | 18 | 2 | 22 (52%) |
| 2 — Visitor Registration & Pass Generation | 45 | 12 | 0 | **33 (73%)** |
| 3 — Entry Verification & ACS Integration | 55 | 13 | 5 | 37 (67%) |
| 4 — Reporting & Notification | 36 | 19 | 4 | 13 (36%) |

Phase 2 is the least contaminated by unbacked scope, which is exactly why it is the phase that can
be delivered and demonstrated while TODO-01 remains open. Phase 4 is the most — only a third of it
is authorized today.

### Named unbacked artefacts

| Artefact | Backing | Verdict |
|---|---|---|
| EPIC-01 Engineering Platform | *none* | ✅ Approved enabler — NFR-MNT-01, NFR-SCL-01, NFR-AVL-01 |
| EPIC-04 Configuration & Master Data | *none* | 🔴 **Unbacked** — TODO-01, D-05 |
| EPIC-10 Host Management | *none* | 🔴 **Unbacked** — D-13 |
| EPIC-19 Analytics & Export | *none* | 🔴 **Unbacked** — TODO-16, D-03 |
| F-14.4 Manual override | *none* | 🔴 **Must not be built** — TODO-04, D-06 |
| F-05.1 Append-only audit log | *none* | ⚠️ TDD-only — TODO-01 |
| F-03.1 / F-03.3 Role & permission model | *none* | ⚠️ TDD-only; **but NFR-SEC-01 does require RBAC** |
| F-17.3/4/5 Host notify, reminders, alerts | *none* | ⚠️ TDD-only — TODO-01 |
| `visitors.id_document_ref` | *none* | 🔴 **Unbacked** — TODO-13 |
| `visitor_status.no_show`, `notify_channel.in_app` | *none* | 🔴 **Unbacked** — D-07 |
| `credential.override`, `report.export` permissions | *none* | 🔴 **Unbacked** — D-12 |
| Edge gateway gate verification | *contradicts SRS §1.2* | 🔴 **Out of scope** — D-08, TODO-19 |

---

## PR-time traceability contract

Every pull request must:

1. Name its user story in the title — `feat(visitor): submit entry request (US-07.1.1)`
2. Link the story issue → feature → epic → requirement
3. Add a row to the delivery log below
4. Update documentation

**A PR that cannot name a requirement does not merge.**

## Delivery log

*Populated as work completes. One row per merged user story.*

| Story | Requirement | Tasks | Key modules | Tests | PR | Merged | Release |
|---|---|---|---|---|---|---|---|
| US-01.1.1 | ENABLER — NFR-MNT-01 (SRS B1) | T-01.1.1.1/2/3 | `.gitattributes`, `.github/CODEOWNERS`, `.github/branch-protection/`, `scripts/{apply,verify,test}-branch-protection.sh` | unit 22/22; integration 2/2 branches; AC-4 proven live (GH006) | [#27](https://github.com/mamunulhasan/pantropi-vms/pull/27) | 2026-07-19 | 0.1.0 |
| US-01.1.2 | ENABLER — NFR-MNT-01 (SRS B1) | T-01.1.2.1/2 | `scripts/lint-commit-message.sh`, `scripts/install-git-hooks.sh`, `.github/workflows/commit-lint.yml`, `.github/branch-protection/` | unit 22/22; check required + exercised on own PR | [#29](https://github.com/mamunulhasan/pantropi-vms/pull/29) | 2026-07-19 | 0.1.0 |
| US-01.2.1 | ENABLER — NFR-MNT-01, CON-01, CON-02 (SRS B1) | T-01.2.1.1/2/3 | `apps/vms-api/` — 5 Gradle modules, 34 context packages, ClockPort/SystemClockAdapter | unit 8/8 (domain purity); integration 3/3 (health UP, no business endpoints, port wiring) | [#30](https://github.com/mamunulhasan/pantropi-vms/pull/30) | 2026-07-19 | 0.1.0 |
| US-01.2.2 | ENABLER — NFR-MNT-01, CON-01, CON-02 (SRS B1) | T-01.2.2.1/2/3 | `vms-architecture-tests` (13 ArchUnit rules), `.github/workflows/architecture-fitness.yml` | 13/13 rules pass; AC-4 violation injected → build failed naming class → removed → green; check required on both branches | [#31](https://github.com/mamunulhasan/pantropi-vms/pull/31) | 2026-07-19 | 0.1.0 |
| US-01.5.1 | ENABLER — NFR-MNT-01 (SRS B1) | T-01.5.1.1/2/3 | `db/migration/V1__baseline.sql`, `V2__reference_seed.sql`, Flyway wiring in `vms-bootstrap` | 5/5 ITs on real PostgreSQL (Zonky): schema-diff, no-op rerun, idempotent seed, failure naming, checksum tamper abort | [#32](https://github.com/mamunulhasan/pantropi-vms/pull/32) | 2026-07-19 | 0.1.0 |
| US-01.4.1 | ENABLER — NFR-MNT-01 (SRS B1) | T-01.4.1.1/2/3 | `.github/workflows/{ci,codeql}.yml`, JaCoCo gate `gradle/coverage.gradle` | 9 required checks live; coverage gate green at 80/60; seeded-violation proofs on draft PR | #33 | pending | 0.1.0 |
| **US-02.1.1** | **FR-ADM-02 (SRS B1)** · NFR-SEC-01 | login use case + PBKDF2/HMAC-JWT adapters + JDBC user directory + `/api/v1/auth/login`,`/me` | 7 crypto unit + 4 login ITs on real PostgreSQL (success, /me, tampered token, uniform failure) | #37 | pending | 0.2.0 |
| **US-02.4.1** | **FR-ADM-01 (SRS B1)** | V3 MASTER_ADMIN grants + BootstrapAdministrator + MasterAdminPolicy + first-run runner | 6 unit + BootstrapAdminIT (migrate→bootstrap→login end-to-end) + MigrationIT V3 grants; 48/48 green | #38 | pending | 0.1.0 |
| **US-02.1.2** | **FR-ADM-02 (SRS B1)** · NFR-SEC-01 | V4 sessions + SessionManager (rotate/replay/logout) + SessionStore/AuditTrail JDBC adapters + `/auth/refresh`,`/logout` + revocation in interceptor | 5 unit + SessionIT (rotate, replay→family-revoke+audit, logout→401); 55/55 green | #39 | pending | 0.1.0 |
| **US-02.2.1** | **FR-ADM-02 (SRS B1)** · NFR-SEC-01 | V5 SYSTEM_ADMIN grant + UserAdministration use case + JDBC user/permission adapters + `/api/v1/admin/users` CRUD + AdminAuthorizationInterceptor (user.manage) | 6 unit + UserAdminIT (create/list, dup 409, deactivate→session revoke, non-admin 403+audit); 65/65 green | #40 | pending | 0.1.0 |
| **US-02.2.2** | **FR-ADM-02 (SRS B1)** | V6 activation_tokens + UserImport (preview/execute) + AccountActivation + TransactionRunner + `/admin/users/import`,`/auth/activate` | 7 unit + UserImportIT (preview→confirm→activate→login, password-column 422, conflict 409); 74/74 green | *(local, unpushed)* | pending | 0.1.0 |
| **US-03.2.1** | **NFR-SEC-01 (SRS B1)** · OWASP ASVS V4 | `AuthorizationInterceptor` on `/**` + `@RequiresPermission`/`@RequiresAuthentication` + `PublicRoutes` allowlist + CORS (no wildcard) | RouteAuthorizationCoverageIT (2) — planted route caught by name; ApiAuthorizationIT: 401 unauth, 403 tenant + no write, principal-from-token, revoked→401 | *(local)* | pending | 0.1.0 |
| **US-03.2.2** | **NFR-SEC-01 (SRS B1)** · OWASP ASVS V4/V7 | RFC 7807 `ProblemDetails` + `GlobalExceptionHandler` + `CorrelationIdFilter` + denial audit (`recordSecurityDenial`) + Micrometer denial metric | ApiAuthorizationIT: uniform body, 403≡404 indistinguishable, audit w/o PII, anonymous denial, correlation id; 86/86 green | *(local)* | pending | 0.1.0 |
| **US-02.3.1** | **FR-ADM-02 (SRS B1)** · NFR-SEC-01 · OWASP ASVS V2 | V8 `login_attempts` + rotation columns · `PasswordPolicy` (length-primary, denylist, predictability) · `ChangePassword` + `AccountRecovery` · `LoginAttemptStore`/`CredentialStore` JDBC adapters · lockout in `AuthenticateUser` · `POST /auth/password`, `/admin/users/{id}/{unlock,reset-password}` · forced-change gate in `AuthorizationInterceptor` | 30 unit (policy, lockout, change, recovery) + AccountSecurityIT (13): lock at threshold, locked≡wrong≡unknown byte-identical, unknown username counted, success resets, change revokes all sessions, weak 422 w/o echo, admin unlock 403/204, reset issues token and leaves the hash untouched, forced change confines the session and survives refresh; 162/162 green | *(local)* | pending | 0.2.0 |
| **US-04.8.1** | `TDD-DERIVED` FR-SET-01 — authorised as an approved enabler by [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `SettingsCatalogue` (fixed key list, types, defaults) · `SystemSettings` use case with WhatsApp guard + before/after audit · `JdbcSettingsStore` (jsonb type preserved on write) · `GET/PUT /api/v1/admin/settings` split across `masterdata.view` / `settings.manage` · `vms.masterdata.enabled` feature flag | 22 unit + SystemSettingsIT (10): catalogue≡seed both directions, seeded listing, change→jsonb+actor+audit, jsonb keeps number/boolean, WhatsApp enable 409 citing TODO-05 and audited, disable still works, `masterdata.edit`→403+audit, unknown key 404, wrong type 400; 198/198 green | *(local)* | pending | 0.2.0 |
| **US-04.8.2** | `TDD-DERIVED` FR-SET-01 · `ENABLER` vs NFR-SEC-01 (SRS B1) — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `SettingValues` typed accessor (fails loudly on a bad stored value rather than falling back) · `CachingSettingsStore` decorator, invalidated on write · secret-valued marker + `SettingsCatalogue.disclose` redacting API **and** audit · `CredentialShape` heuristic refusing credential-shaped writes ahead of the type check | 22 unit (accessor, cache, credential shapes, redaction) + 3 IT: cache invalidation through the real stack, credential value 409 and absent from audit, nothing redacted today; 242/242 green | *(local)* | pending | 0.2.0 |
| **US-04.1.1** | `TDD-DERIVED` FR-CFG-02 — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | **The shared master data pattern, built once**: `MasterDataText` domain rules · `MasterDataStore<T>` port (no delete anywhere) · `MasterDataDefinition<T>` · generic `MasterDataAdministration<T>` with audit built in · applied to `Building` · `JdbcBuildingStore` (constraint-driven conflicts, allow-listed sort) · `/api/v1/admin/buildings`. Also fixes the catch-all turning `HttpRequestMethodNotSupportedException` into 500 | 9 domain + 9 pattern unit + BuildingAdminIT (10): create→audit, paged/filtered/searchable list, deactivate retains row, **duplicate code under a 6-thread race**, rename conflict, no delete route, viewer 403+audit, invalid code 400, 401, 404s; 275/275 green | *(local)* | pending | 0.2.0 |
| **US-04.2.1** | `TDD-DERIVED` FR-CFG-03 — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `Floor` domain record (building required, level bounded) · `FloorDefinition` · `JdbcFloorStore` — **active-parent enforced inside the INSERT** via `WHERE EXISTS`, composite `(building_id, code)` conflict translation, `level_no NULLS LAST, code` ordering, update scoped by building · nested `/api/v1/admin/buildings/{buildingId}/floors`. Port gained `Query.parentId` and `InvalidParent`, both reused by the next child entity | 7 domain + FloorAdminIT (12): create→audit w/ parent, ordering w/ unnumbered last, list scoped to building, same code in another building OK, deactivated parent 400, missing parent 400, no delete route, wrong parent 404, update cannot relocate, viewer 403, level bounds; 299/299 green | *(local)* | pending | 0.2.0 |
| **US-04.5.1** | `TDD-DERIVED` FR-CFG-06 — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `VisitorType` + `VisitorTypeDefinition` + `JdbcVisitorTypeStore` + `/api/v1/admin/visitor-types` — the pattern's cheapest entity, ~3 small files. Also extracts `MasterDataQuery` (filter/paging/args assembly) at the third adapter and refactors the building and floor stores onto it | 3 domain + 7 `MasterDataQueryTest` + VisitorTypeAdminIT (9): seeded four present, create→audit, dup 409, deactivate, no delete route despite `ON DELETE SET NULL`, search, size cap, viewer 403+audit, 400/401/404; 319/319 green | *(local)* | pending | 0.2.0 |
| **US-04.6.1** | `TDD-DERIVED` FR-CFG-07; consumed by **FR-VMS-11 (SRS B1)** and **FR-VMS-13 (SRS B1)** in Phase 2 — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `CredentialType`/`RestrictionType` domain enums with **two distinct entry points** — `parse` (input, lists permitted values) and `fromDatabase` (throws `UnknownDatabaseValue`, never defaults) · `PassType` validating positive+bounded hours in the domain, not only the DB `CHECK` · `JdbcPassTypeStore` with `::vms.<enum>` casts · full-state audit · `/api/v1/admin/pass-types` | 12 domain + PassTypeAdminIT (8): seeded round-trip field by field, create/update audit complete states, unpermitted enum 400 listing permitted, non-positive hours 400, deactivate keeps row, dup 409 / viewer 403 / no delete / 401, and **enum drift — `ALTER TYPE … ADD VALUE 'nfc'` then read fails loudly instead of silently reading as `qr`**; 343/343 green | *(local)* | pending | 0.2.0 |
| **US-04.7.1** | `TDD-DERIVED` FR-CFG-08 — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | **Deliberately off the shared pattern**: no code, no `is_active`, keyed by date, genuinely deletes · `Holiday` + `HolidayCalendarStore` + `HolidayCalendar` use case · delete audited **before** the row is removed, since the audit is then the only record it existed · validate-then-apply bulk import in one transaction with per-row report · `/api/v1/admin/holidays` incl. the only `DELETE` in master data | 12 unit + HolidayCalendarIT (10): add→audit, working exception distinguishable, year+range ends inclusive, delete removes row and keeps prior state, dup date 409, clean import applies 3, one clash writes nothing, in-set duplicate caught pre-DB, viewer 403 on add+delete, 400/401/404; 365/365 green | *(local)* | pending | 0.2.0 |
| **US-04.4.1** | `TDD-DERIVED` FR-CFG-05; consumed by **FR-ADM-01 (SRS B1)** and **FR-ADM-02 (SRS B1)** — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `Reception` + floor-scoped store · `ReceptionAdministration` — atomic central transfer under `FOR UPDATE`, auditing the **previous holder**; deactivation guarded and dependent count reported · **`MasterAdminPolicy` finally wired into `UserAdministration.deactivate`** (it was a bean, unit-tested, and called by nothing) · both guards re-expressed as one invariant over the post-state and reading through the same locked row set | 9 `MasterAdminInvariantTest` + 3 `UserAdministrationTest` + ReceptionAdminIT (9): create never central, floor-scoped dup 409, transfer needs confirm + audits predecessor + exactly one central, filters/total, deactivation keeps `users.reception_id`, stranding 409, **last-admin guard now actually refuses**, **two concurrent deactivations → exactly one 204 and one 409**; 420/420 green | *(local)* | pending | 0.2.0 |
| **US-03.1.1** | `TDD-DERIVED` FR-USR-02 · `ENABLER` vs NFR-SEC-01 (SRS B1) — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | **`V9__role_permission_grants.sql` — the authoritative matrix, completing the lift of D-15** · `Permissions` curated constants + `PermissionSet` (`NONE` is an answer, not a failure) · `RoleGrantStore` + `EffectivePermissions` (inactive user → empty set) · dev grant matrix **removed** from `LocalDevSeeder` · seven ITs re-pointed at the real matrix with their hand-seeded grants deleted | 8 unit + RoleGrantMatrixIT (7): roles/permissions exact, **constants ≡ `vms.permissions` both directions**, matrix exact role-by-role, unbacked three granted to nobody, idempotent re-run, no grant of an unknown code; 435/435 green | *(local)* | pending | 0.2.0 |
| **US-03.3.1** | `TDD-DERIVED` FR-USR-02 · `ENABLER` vs NFR-SEC-01 (SRS B1) — [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `RoleAdministration` — grant edits on the five seeded roles only (no create-role, no create-permission; TODO-01) · `GrantVersion` **content-derived** optimistic token · administrative-lockout guard evaluated against the *resulting* matrix under a row lock · `credential.override` refused at the API as well as the migration · `GET/PUT /api/v1/admin/roles` | 16 unit + RoleAdminIT (10): overview w/ versions+counts+catalogue, change audited both sets, **live session sees revoked grants on its next request without re-login**, lockout 409 changes nothing, stale version 409 carrying current state, unbacked 400 naming TODO-04, unknown code 400, 403 w/o `user.manage` + audit, 401/404, creation not routed; 460/460 green | *(local)* | pending | 0.2.0 |
| **US-03.4.1** | `BLOCKED` TODO-14 — mechanism justified against NFR-SEC-01 (SRS B1); policy per [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md), recorded in [ADR-0005](../adr/0005-tenant-and-floor-scoping-seam.md) | `ScopedEntity` + sealed `ScopeFilter` + `ScopeContext`/`ScopeContextLifecycle` ports + `ScopePolicy` (the only place isolation is decided) · request-scoped `RequestScopeContext` with **lazy** resolution from `vms.users` and clearing in `afterCompletion` · `VisitorScopeSql` translates per table · `findById` on visitor requests now filtered · **`ScopingRulesTest` fails the build naming any adapter that bypasses the policy — verified by seeding a violation** | 12 unit + ScopeContextIT (10): resolves from the database not the token, per-thread, **no residue on a pooled thread**, deactivated user → DENY_ALL, policy end-to-end; 486/486 green | *(local)* | pending | 0.2.0 |
| **US-07.5.1** | `TDD-DERIVED` TDD §4.2 — the integrity control behind **FR-VMS-02 (SRS B1)**; authorised by [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md) | `RequestTransitions` declarative table + single guard · `VisitorCascade` honouring terminal visitor states · `reject(approver, reason)` with the mandatory reason as an **aggregate invariant** · two fitness rules (status mutators stay package-private; only the aggregate consults the table). **Fixed two defects in US-07.1.1's aggregate**: `approved → cancelled` was refused though legal, and an individually-cancelled visitor was resurrected by approval | 22 unit incl. **every source×target cell against an independently-written expectation**, terminal-visitor cases per status, exhaustiveness over both enums, blank/over-long reason; 2 fitness rules | *(local)* | pending | 0.2.0 |
| **VJ-1** (US-07.1.1, US-07.1.2, US-07.1.3, US-07.6.1, US-07.6.2 — screens) | **FR-VMS-01 (SRS B1)**; ADR-0006. Depends on the CORS enabler above | The tenant journey: submit, track, amend, withdraw · **`AppShell` extracted** from the admin layout — the alternative was a second copy of the session guard, bfcache re-check and redirect logic, security-relevant code whose copies drift invisibly until someone's back button shows a stale console; one implementation, parameterised by nav registry + entry permissions · **`(tenant)` route group** entered on `visitor.request` · `/visits` list is a **conditional GET**: the ETag from the previous read returns as `If-None-Match` and a **304 leaves the rendered rows untouched** (US-07.6.2 realised end-to-end for the first time); the validator is keyed to the exact filter+page, since a filter change makes the old one meaningless · `/visits/new` submit form with client-side bounds mirroring the server's, **naming the offending visitor by position** — something the API's `{error:"invalid",detail:"visitor email"}` cannot do, as it names the field kind and not the row · `/visits/{id}` shows the **outcome first** (decision, decider's display name, reason) then amend/withdraw · **`homeFor(permissions)`**: one place decides where a session lands, fixing a real defect — both the front door and the post-sign-in redirect hardcoded `/admin`, which walked every tenant into the no-access wall the moment a second area existed · `lib/datetime.ts` converts `datetime-local` ↔ instant through the browser's zone (a naive implementation books every visit six hours early in +06) | **The amend design is the interesting decision.** The API replaces the visitors array wholesale, and the detail response deliberately omits visitor email and phone (TODO-13) — so pre-filling an editor from what is readable and sending it back would **silently erase every visitor's contact details** on an unrelated window edit. Amend therefore **omits `visitors` entirely** (leaving them untouched), and replacing the list is an explicit opt-in that states the consequence and starts from names alone · **Two API gaps stated, not papered over**: no host endpoint exists anywhere (`vms.hosts` is validated against but never exposed), so requests submit with `hostId` null and the form says so; and `TENANT` cannot list visitor types (`masterdata.view` is not its grant), so no type selector is offered | 24 new Vitest (98 total) + 16 new Playwright (42 total): datetime round-trip in any zone · visitor validation incl. the phone shapes the server strips · `visitError` over field-kind/conflict/422/empty-404 · **e2e proves the browser-only behaviours**: amend leaves as **PATCH**, the list **reads `ETag` and returns `If-None-Match`**, a 304 does not blank the list, replacing visitors is opt-in with empty contact fields, the front door routes each role to its own area and refuses the other · **the mock had to add `Access-Control-Expose-Headers`** — until it did, the browser answered `null` for `ETag`, independently demonstrating why the enabler above was necessary · fixed two flaws in UI-5's own login test found on the way: a bare `getByRole("alert")` also matches Next's route announcer (so it could pass while sign-in quietly succeeded, scanning the wrong page), and a specific `page.route` registered **before** the catch-all never runs | *(local)* | pending | 0.2.0 |
| **UI-5 / F-06.4** | `TDD-DERIVED` F-06.4, approved enabler per [ADR-0006](../adr/0006-frontend-stack-and-auth.md) (amended here); carries the deferred **T-06.3.2.3** | Accessibility gate as its own CI job: Playwright drives a **production build** (the dev overlay's DOM never ships, so a violation against it is a false alarm that teaches people to ignore the gate) with the API **stubbed at the browser's network boundary** — no Java, no PostgreSQL, no seed — and axe-core scans **13 routes plus three dialog states while open** (a modal is where labelling and focus bugs live) · fails on **critical/serious** only, advisory tier printed: a gate that fires on untriaged hints gets switched off, and a gate people switch off protects nobody (threshold recorded as an ADR amendment) · structural asserts beyond axe: one `h1` per screen, `main` + named `navigation` landmark, skip link **reachable on first Tab and visible when focused** · **the gate immediately found two real defects**: the UI-0 placeholder `/admin` page shipped its own `<main>` **nested inside the shell's** (duplicate landmark) and its copy still said screens were coming — rewritten as a real permission-filtered console home, one name (“Administration”) across nav label, `h1` and tab title · **T-06.3.2.3 client tamper**: permission-less principal sees no nav, gets the no-access state on a typed URL, and a DOM-injected nav link still yields nothing — asserting **no admin request is issued at all** (the guard sits above the screen), with the mirror-image test proving the mechanism is permission-driven · mock models **logout invalidating refresh**, without which the bfcache test would assert nothing · scope stated in-file and in the ADR: server-side authorization stays proven by `ApiAuthorizationIT`, never by this suite · `docs/project/16-accessibility.md` — manual checklist (keyboard, screen reader, 200%/320px reflow, colour-not-sole-carrier) referenced from a new PR-template section | 26 Playwright tests, all green first run with **zero axe findings at any impact level**; the gate was then verified to *fail* by injecting an alt-less image (`image-alt`/critical) — a gate that cannot fail is worse than none · 74 Vitest tests unchanged | *(local)* | pending | 0.2.0 |
| **UI-4b** (US-04.3.1, US-04.4.1, US-04.7.x, US-02.2.1/2, US-03.3.1 — screens) | **FR-CFG-04/05, FR-CFG-08, FR-USR-01/02 (SRS B1)** via the merged APIs; ADR-0006 | **Tenants**: deactivation fetches the dependent count first and states it in the confirm — it informs, never blocks; floor picker narrows building→floor with "(keep current floor)" on edit · **Receptions**: the central designation moves through the API's two-step — the 409 `confirmation_required` naming the current holder IS the confirm dialog, so the admin always sees who loses it; create requires a floor and starts non-central; edit is code/name only (a reception can never move floors); `would_strand_master_admin` surfaces verbatim · **Holidays**: year view over the bare date-ordered array (the year selector is the pagination); the one real delete, confirmed naming the record (audit keeps the only trace); bulk import textarea (`YYYY-MM-DD, Name[, working]`) — the server is all-or-nothing, so the 409 per-row report renders as the preview: nothing written, fix the lines, resubmit · **Users**: create is the API's two-call flow (POST issues no token by design → reset-password does) ending in a shown-once activation-token dialog; assignment-only edit; deactivate/reactivate/unlock/reset behind confirms; `last_master_admin`'s sentence-in-the-field-slot wire quirk absorbed in `errorMessage`; CSV import preview→execute (raw `text/csv`, not multipart) with explicit opt-in to skip conflicts — the 409 is the preview shape again · **Roles**: grant matrix (catalogue × roles with active-user counts); per-role save carries the **version token**; a 409 `stale_version` advances the stored version, keeps the draft, and **re-asks showing the current set — never a silent retry** (content-derived token → A-B-A harmless); UNBACKED permissions render disabled-not-hidden with the TODO-04/16 note · nav gains Tenants/Receptions/Holidays (`masterdata.view`) and Users/Roles (`user.manage`) | 11 new Vitest tests (74 total): holiday parser (flag, commas-in-names, line-numbered errors, blank-line numbering, empty input) · `userListQuery` param names (role, raw column sorts) · `staleVersionOf` extraction + null-for-everything-else · `last_master_admin` quirk · no-message `{error,field}` still answers a sentence | *(local)* | pending | 0.2.0 |
| **UI-4a** (US-04.8.1/2, US-04.1.1, US-04.2.1, US-04.5.1, US-04.6.1 — screens) | **FR-SET-01, FR-CFG-02/03/06/07 (SRS B1)** via the merged APIs; ADR-0006 | First real admin screens on the UI-3 nucleus · **`lib/admin-api.ts`**: typed client transcribed from the controllers — `{items,page,size,total}` envelope (deliberately not shared with the visitor endpoints' `{content,totalElements}`), `errorMessage`/`fieldError` absorb **all three failure shapes** (ad-hoc `{error,field,message}`/`{error,key,message}`, problem+json, empty-body 404) so no screen parses a payload · **`useAdminList`**: the URL is the source of truth for search/filter/sort/page — a filtered page-3 view survives refresh and pastes to a colleague; non-paging changes reset to page 0; renders the **echoed** page/size (the server silently clamps) · **Settings**: catalogue-ordered table, typed edit control per declared type (BOOLEAN select / INTEGER number / STRING text) with the value travelling as a string (the wire contract); `usingDefault` badge; `secret` renders "Set (not shown)" — the asterisks are a marker, not data; the 409 `change_refused` texts (credential-shaped value, WhatsApp/TODO-05) surface verbatim — they were written for this screen; edit affordance needs `settings.manage`, viewing `masterdata.view` · **Buildings + nested floors**: CRUD dialogs, deactivate/reactivate behind confirms that **name the record**, no delete anywhere (the API has none); floors breadcrumb resolves the building so a guessed URL 404s (the floor list alone answers an empty page); level number optional for mezzanines · **Visitor types** and **Pass types** (enum selects over the exact wire values `qr`/`rfid`, `time_bound`/`one_time` — the database's spelling; validity 1–744h) · nav registry gains all four destinations under `masterdata.view` · edit affordances hidden without `masterdata.edit` (usability — enforcement stays server-side) | 14 new Vitest tests (63 total): `listQuery` defaults/booleans/encoding · `errorMessage` over all three shapes + never-`Error#message` · `fieldError` field-vs-key naming · `useAdminList` URL-parse → fetch, filter-resets-page, paging-keeps-filters, default-sort-stays-out, reload-is-not-navigation | *(local)* | pending | 0.2.0 |
| **UI-3 / US-06.2.1** | `TDD-DERIVED` F-06.2, approved enabler per [ADR-0006](../adr/0006-frontend-stack-and-auth.md) | Component nucleus in `components/ui/`, hand-rolled on native elements (no component library was authorised; the ACs are behaviour we must own) · **`Field`/`Input`/`Select`/`Textarea`**: label association, error adjacent + `aria-describedby` so the announcement carries the reason, `aria-invalid`, and `focusFirstInvalid` finds exactly the marker `Field` sets — the two halves cannot drift · **`Button`**: token variants, `busy` disables + `aria-busy`, and `type` defaults to `button` (not the HTML `submit` default) so a dialog's cancel can never submit a form · **`Dialog`**: focus trap (Tab/Shift+Tab cycle), Escape closes, focus **restores to the opener**; `ConfirmDialog` (AC-6) descriptions must name the record and **initial focus is Cancel — the destructive control is never the default**, so an early Enter means "keep it" · **`Toast`**: `role=status` polite live region — a toast never interrupts; auto-dismiss (6s) plus always a manual dismiss · **`DataTable`** (AC-3): controlled over **one server page** — it has no way to hold a full dataset; reports `onPageChange`/`onSortChange` upward for the screen to translate into `page/size/sort` params; `aria-sort` on the active header; zero-based like the API · **`ErrorState`** (AC-5): problem-body `detail` + correlation id to quote at support, **never the raw payload or `Error#message`** (written for a developer log) · new `--color-scrim` token for the dialog backdrop | 26 new Vitest tests (49 total): field association/adjacency/clean-control/hint+error/focus-first-invalid-in-document-order · dialog focus-in, both wrap directions, Escape + **restore-to-opener**, labelled-by-title, confirm-focuses-Cancel-not-destructive · table one-page render, `aria-sort`, toggle asc→desc, bounds-disabled paging, empty slot · toast live region, manual + timed dismiss · error state shows detail+id and **provably not the payload's other keys** | *(local)* | pending | 0.2.0 |
| **UI-2 / US-06.3.2** | `TDD-DERIVED` F-06.3 / T-06.3.2.2, approved enabler per [ADR-0006](../adr/0006-frontend-stack-and-auth.md); consumes the US-06.3.2 API half | `lib/permissions.ts` transcribes `Permissions.java` one for one — a misspelled code would hide a nav item for everyone, quietly, so codes are compile-checked constants · `hasAny` matches the server's `@RequiresPermission` **any-of** semantics and fails closed (no session, empty held set, empty requirement → false) · navigation is **data**: `lib/nav.ts` registry, each destination declaring its permissions; `visibleNavItems` is the single filter the shell renders through — an unheld item is **absent, never disabled** (AC-1): a greyed-out entry is a catalogue of the system's capabilities handed to a shoulder-surfer · admin-area entry = any of `user.manage`/`settings.manage`/`masterdata.view`; a principal holding none (or nothing at all) gets the explicit `AccessDenied` state with the header kept so sign-out stays reachable (AC-4/AC-5), never an empty shell · header shows `displayName` · store parse **fails closed on an older API payload**: username as name, `[]` permissions, no crash — none of which is an authorization decision; every call is enforced server-side | 10 new Vitest tests: `hasAny` any-of + all fail-closed shapes, filtered nav keeps exactly the justified items, unheld item absent from the result, TENANT set yields zero admin nav, admin-entry membership per permission, store carries `displayName`+`permissions`, old-payload fail-closed (23 total) | *(local)* | pending | 0.2.0 |
| **US-06.3.2 (API)** | `TDD-DERIVED` F-06.3 / T-06.3.2.1, approved enabler per [ADR-0006](../adr/0006-frontend-stack-and-auth.md) | `GET /api/v1/auth/me` now delivers the profile the portal boots from: `displayName` and the **sorted effective permission set**, in one round trip (a lazily-fetched nav would render, reflow, and hand an observer a timing oracle) · new `CurrentUser` use case composing `UserAdministrationStore.findById` + `EffectivePermissions.forUser` — **live data, not token data**: the token carries identity, never authority, so a grant edit or deactivation lands on the next profile read · fails closed in every direction: deleted user → username as name + `[]`; deactivated → `[]` (same answer the authorization interceptor gives) · sorted codes so a client "did-my-permissions-change" check is a string compare | 4 `CurrentUserTest` (display name + sorted set, **stored-role-beats-token-role**, deleted fails closed, deactivated fails closed) · `AuthLoginIT` asserts the payload shape · `ApiAuthorizationIT` asserts against the **real migrated grant matrix**: SYSTEM_ADMIN carries `user.manage`+`audit.view`, TENANT carries `visitor.request` and not `user.manage` | *(local)* | pending | 0.2.0 |
| **UI-1 / US-06.3.1** | `TDD-DERIVED` F-06.3, approved enabler per [ADR-0006](../adr/0006-frontend-stack-and-auth.md); closes the loop on **US-02.3.1 (SRS B1)**'s forced password change | Login page (branded, labelled, **one neutral failure message** — the API made wrong-password/locked/unknown indistinguishable and the UI must not undo it) · **auth-only BFF**: `app/api/session/{login,refresh,logout}` keep the refresh token in an HttpOnly/SameSite=Strict cookie **path-scoped to /api/session**, so page JS never sees it and it never travels with a data request (AC-3) · access token in a framework-free in-memory store; **single-flight refresh** — concurrent 401s join one promise (AC-6) · `apiFetch` refresh-and-retry-once, then **one** redirect to login with the location preserved (AC-1/AC-5) · `safeNextPath` allowlists the attacker-writable `next=` param (no `//host`, no schemes) · shell layout with skip-link, header, nav and content regions; guard holds during session resume instead of flashing; **`pageshow`+`persisted` re-check so a bfcache restore after logout shows nothing authenticated** (AC-4) · `mustChangePassword` routes to the forced-change screen, which is the only way forward because the API refuses everything else · logout: API revocation **first**, cookie clear second, and client state clears even when the API is down | 13 Vitest tests: memory-only session (storage + cookie empty), refusal keeps nothing, **3 concurrent refreshes → one request**, sequential refreshes are fresh, refused refresh clears state, logout ordering (revoke before cookie), logout survives a dead API, 6 `safeNextPath` shapes · CI gains the test step | *(local)* | pending | 0.2.0 |
| **UI-0 / US-06.1.1 (core)** | **FR-ADM-03, CON-03 (SRS B1)**; NFR-USA-01 · stack choices recorded in [ADR-0006](../adr/0006-frontend-stack-and-auth.md) | First frontend code: `apps/vms-web` (Next.js 15, App Router, TS strict, Tailwind v4, npm) · **design tokens as CSS custom properties** in `styles/tokens.css` — the single source; Tailwind's `@theme inline` *maps* the tokens rather than defining a palette, so a rebrand is a token edit · typed accessor `lib/tokens.ts` (unknown token = compile error) · branding from `NEXT_PUBLIC_*` env with a neutral text fallback when the logo asset fails (AC-2/3/6) · **`scripts/check-contrast.mjs` parses tokens.css itself** and fails the build naming any text pairing below WCAG 2.1 AA 4.5:1 (AC-4) — wired into the new `frontend` CI workflow (lint, typecheck, token contrast, build) · route groups `(auth)`/`(admin)` scaffolded with placeholders · **fixed the abandoned Node 20 install**: nvm's v20.18.0 shipped a partially-extracted npm (`@npmcli/config` missing); replaced from the official 10.8.2 tarball · deferred + tracked in ADR-0006: token-literal lint rule (T-06.1.1.3), visual regression (T-06.2.1.3) | 14 contrast pairings green; `lint`+`tsc`+`build` clean; CI workflow runs all four on any `apps/vms-web/**` PR | *(local)* | pending | 0.2.0 |
| **US-08.1.3** | **FR-VMS-03 (SRS B1)** | Visitor-level `amendVisitor`/`cancelVisitor`/`reschedule` on the aggregate — a corrected visitor **keeps their id and status** (a credential references the id; a new id is a different person downstream) · cancel-the-last-active cascades to the request through the ordinary `cancel()`, so the state machine and the revocation flag apply · **AC-5 falls out of the scope seam**: `OwnReception` over tenant-keyed visitor tables now means *the tenants on my floor* (ADR-0005 amended) instead of `FALSE`, so a foreign floor's record 404s through the same predicate as every other IDOR case — floor-scoped, not creator-scoped · `savePreArrivalChange` updates visitor rows **in place**: `credentials.visitor_id` is `ON DELETE CASCADE`, and the delete-and-reinsert `saveAmendment` uses would silently destroy the credential row AC-3 reads · `IssuedCredentials` port implemented in **`infrastructure.credential`** (first code in the owning context); `requested` counts as live, or an in-flight pass arrives after the withdrawal · revocation in the same transaction as the cancellation — a rolled-back cancel must not revoke, a landed one must · `PATCH /api/v1/pre-registrations/{visitorId}` + `/cancel` under `visitor.register` | 17 `PreArrivalChangesTest` (**every visitor status for amend and cancel via `@EnumSource`**, identity preserved, last-active cascade incl. approved→`cancelledAfterApproval`, checked-in keeps the request alive, reschedule legal submitted+approved and refused mid-visit) + 12 use-case (partial coalescing, flags-not-content audit, **losing cancel revokes nothing**, one revocation with a live credential) + PreRegistrationMaintenanceIT (10): in-place edit same row, checked-in 409 both ops, last-visitor cascade leaves the queue, one-of-two stays, **inserted credential → one revocation event + `followedIssuance:true`**, no credential → none, **two-floor 404 byte-identical + audited**, same-floor colleague may edit, tenant 403, 401 | *(local)* | pending | 0.2.0 |
| **US-08.1.1** | **FR-VMS-03 (SRS B1)** ⭐ *first EPIC-08 story — the second entry path* | `PreRegisterVisitor` producing **the same aggregate** a tenant submission does, per T-08.1.1.1 — a parallel model would need its own state machine, cascade and approval path · `ReceptionScope` port (declared by visitor, implemented by master data, so `vms.receptions` is never read from the visitor context) · `RegistrationPolicy` port for the AC-5 grace period, `V14` seeds the key so an administrator can actually see and change it · **AC-2 is not implementable as written**: `tenants.floor_id` is non-unique, so the reception→floor→tenant chain does not identify one tenant on a shared floor. Interim rule — derive when unambiguous, otherwise the caller names one **validated against their own floor** (403 + audited denial otherwise); recorded as **TODO-20** rather than guessed, and the audit records `tenantDerived` either way · `POST /api/v1/pre-registrations` under `visitor.register` | 15 `PreRegisterVisitorTest` (same aggregate, derive vs name vs refuse, unstationed, empty floor, grace boundary at 60/61 min, configurable grace, inverted window, ids-only audit and event, **the command has no field for a floor or reception at all**) + PreRegistrationIT (17): request+visitor with the appointment window on the visitor row, reaches the FM Admin queue, single-tenant floor derives, **shared floor demands a choice**, **foreign floor 403 + audited**, payload cannot widen the station, unstationed 409, stale appointment 422 and recent one accepted, grace change takes effect, malformed email names the field only, tenant 403, 401 | *(local)* | pending | 0.2.0 |
| **US-07.6.2** | **FR-VMS-01 (SRS B1)** — status visibility · **PARTIAL, deliberately** | **Only `T-07.6.2.3`'s conditional-read half is delivered.** `T-07.6.2.1` (polling hook) and `T-07.6.2.2` (stale-data indicator) are client-side and deferred with all UI; the story's user-visible outcome is therefore **not** delivered. The Redis short-TTL cache is also deferred — no obtainable client (as US-04.9.1/2), so an unchanged list costs **one count query rather than zero**, a stated deviation from the AC · weak `ETag` + `If-None-Match` on `GET /api/v1/visitor-requests`; validator is `count` + `max(greatest(updated_at, created_at))` over the scoped rows, SHA-256'd so it cannot be read back as a description of what the caller may see · **the scope is folded into the validator**, so two tenants with identical counts and timestamps still differ — the AC's cross-tenant bleed made impossible rather than improbable · `Cache-Control: private` + `Vary: Authorization` as an independent second control, because two tenants poll the same URL · filter assembly extracted so the list and its validator cannot filter differently · **known gap, stated not hidden**: a host renamed in master data does not move any request row, so a poll can serve a stale host name until that request next changes | 6 new in TenantRequestsIT: 304 on an unchanged list with an empty body, **a decision invalidates the validator and the next poll carries the new status**, **two tenants never share a validator and the other's ETag never yields 304**, response is `private` and varies on `Authorization`, two filtered views never validate against each other, an unauthenticated poll is 401 not 304 | *(local)* | pending | 0.2.0 |
| **US-07.3.3** | **FR-VMS-02 (SRS B1)** | **Closes a hole earlier stories left**: `GET /{id}` was declared `visitor.request` only, so the approver who has to decide a request could not open the one in their own queue · `@RequiresPermission` becomes **any-of** (`String[]`) — a resource legitimately reachable by two authorities that are not subsets of one another; *what* each caller gets is still the scoping policy's decision at the query, so one handler serves both with no branch to get backwards · detail projection gains **company and visitor-type display name** (not the id) · AC-2: the read is itself audited as `visitor_request.view`. **Found by reading rather than running**: `AuditTrail.record()` wraps its detail into `after_state` and runs it through `safe()`, so the visitor count I first passed would have been mangled *and* would have contradicted T-07.3.3.1's "after_state is null for a read" — passing null is what satisfies it | 11 use-case incl. the audited read and the not-found case leaving no view entry, + ReviewRequestIT (12): approver sees tenant/host/purpose/window/status and every visitor with company and type, **the queue row now opens**, still no contact detail or document ref, `visitor_request.view` names the reader with both state columns null, two readers leave two entries, a 404 leaves none, decided request shows outcome/decider/reason, **missing and out-of-scope byte-identical**, same id 404 for the tenant and 200 for the approver, neither permission 403 with the denial naming `visitor.request or visitor.approve`, 401 | *(local)* | pending | 0.2.0 |
| **US-07.3.2** | **FR-VMS-02 (SRS B1)** | Filters composed **after** the scope predicate, assembled in one place so no combination of parameters can omit it (AC-5) · **`status` defaults to `submitted` and is an explicit opt-in** — the reading that keeps US-07.3.1 AC-3 *and* this story's AC-1 meaningful, recorded on the use case because the two ACs pull against each other · name search over visitor **and** host, `ILIKE` with `%`, `_` and `\\` escaped and an `ESCAPE` clause · a visitor name is **searchable but still never returned** — the queue lists counts, not people · `V13__approval_queue_search.sql` adds `pg_trgm` (in `public`, per V11's rule) and GIN trigram indexes, because a leading-wildcard `LIKE` on a btree is a sequential scan of every visitor in the building · count and page queries share one `FROM`, so a total cannot drift from the rows under it · **fixed: `UnknownStatus` echoed the caller's value into the response and the log**, contradicting T-07.3.2.2 — same defect fixed in US-07.6.1's copy | 11 `PendingApprovalsTest` (default status, every status by value/name, four bogus values, inverted range refused not swapped, open-ended and equal bounds, filters combine, blank search, bound, page bound still applies, **the filter record has no field that could widen scope**) + 7 new in ApprovalQueueIT: conjunctive tenant+range, status opt-in with AC-3 still holding, case-insensitive name search over both tables, **search does not widen the projection**, `%` and `_` literal, injection is text, 400s that do not echo the value, **explicit foreign-tenant filter under the restrictive posture returns nothing** | *(local)* | pending | 0.2.0 |
| **US-07.4.3** | **FR-VMS-02 (SRS B1)**; `TDD-DERIVED` FR-AUD-01 (TDD §4.6) for the audit store | **`V12__audit_trail_integrity.sql`** — append-only enforced by a **trigger that raises** plus a `REVOKE`: the revoke is the grant-level control the AC names but does nothing to a superuser, and a `RULE … DO INSTEAD NOTHING` would make tampering *silently succeed* · **new `audit.view` permission granted to SYSTEM_ADMIN alone** — `report.view` was the tempting reuse and is ungranted pending TODO-16, so borrowing it would have answered that question as a side effect · `AuditProjection` replaces four hand-built JSON payloads with one **allow-list**, so a future `id_document_ref` is absent by default; `Visitor` is never projected, only counted · `DecisionTrail` port + `GET /{id}/history` under `audit.view`, **deliberately not tenant-scoped** (oversight that sees only its own tenant is not oversight) · **US-07.4.1's choice to omit the approval note is superseded** by AC-1's "decision reason where applicable" | 9 `AuditProjectionTest` incl. **a reflective check that no readable value of any visitor reaches the payload, whatever it is called** — stronger than naming today's PII fields, which would pass on the day a new one is added — plus stable key order and escaping + DecisionTrailIT (10): **UPDATE and DELETE refused as superuser**, refusal is loud, inserts still work, chronological trail with actor/action/time/reason, no visitor PII, empty history not 404, **forced audit failure rolls the decision back and the same decision then succeeds**, FM Admin 403 + denial audited, tenant 403, 401, grant is SYSTEM_ADMIN only | *(local)* | pending | 0.2.0 |
| **US-07.1.3** | **FR-VMS-01 (SRS B1)** | `amend()` on the aggregate — validate-everything-then-apply, so a rejected edit leaves nothing half-applied; null means *leave alone*, and clearing a host is explicitly out of scope rather than silently unsupported · `cancel()` records `cancelledAfterApproval`, because withdrawing an approval may mean a credential exists · **`CredentialRevocationRequested` is a separate event, not a flag** — a consumer that forgot to filter would revoke a credential that was never issued · `saveAmendment` reuses the status-as-version compare-and-set; **no `version` column was added despite T-07.1.3.2 asking for one**, because every operation on this aggregate is already legal only from a known state · `PATCH /{id}` and `POST /{id}/cancel` under `visitor.request` · **AC-6: the 404 is now audited** — a probe walking ids leaves a trail even though every answer it gets is identical | 12 `AmendAndCancelRulesTest` (every source state for both, partial edit, empty edit, refused edit applies nothing, revocation flag set only after approval and never by a refused call) + 12 use-case + AmendCancelIT (10): amend persists/audits/announces with visitors replaced and normalised, partial edit, foreign host 400, cancel leaves the queue, **cancel-after-approve emits the revocation signal**, rejected and approved closed to amendment, **approve-vs-cancel race → one 200, one 409, exactly one event, one audit row**, foreign id 404 identical to missing + denial audited, 401 | *(local)* | pending | 0.2.0 |
| **US-07.1.2** | **FR-VMS-01 (SRS B1)** | `EmailAddress` and `PhoneNumber` value objects — normalised once, equal across capitalisation, and **redacting in `toString()`**, which makes "never logged" structural rather than a habit · lower-cased in the *root* locale, so a Turkish default locale cannot split one address into two · `visitor_type_id` captured end to end · `VisitorTypeDirectory` port declared by the visitor context and implemented by **master data**, so no SQL against `vms.visitor_types` leaves its owner; wired on the identity switch, not `vms.masterdata.enabled`, or turning the admin endpoints off would stop visitor submission · unknown and inactive types give one indistinguishable answer · **`V11__citext_resolvable_at_runtime.sql` — the citext extension lived in `vms`, application connections run `search_path = "$user", public`, so the `=` operator was unresolvable and every citext comparison silently fell back to case-sensitive `text` equality**; unique indexes were unaffected (operator class by OID), so the system refused to create `Alice` beside `alice` while also refusing `Alice` a login | 17 `VisitorDetailsTest` (normalisation, locale independence, 8 malformed emails, 6 malformed phones, redaction, interpolation) + 5 use-case + VisitorDetailsIT (9): five fields persist, **citext match on a default-search_path connection**, optional type, inactive/unknown refused identically, one bad type refuses the group, malformed email 400 with the value in **no log line**, oversized name, malformed phone · sharpened `AccountSecurityIT` lockout guard, which had passed only because its literal matched the stored spelling exactly | *(local)* | pending | 0.2.0 |
| **US-07.6.1** | **FR-VMS-01 (SRS B1)** — the phase's primary IDOR surface; restrictive default per [ADR-0004](../adr/0004-master-data-and-rbac-as-approved-enablers.md)/[ADR-0005](../adr/0005-tenant-and-floor-scoping-seam.md) | `VisitorRequestQueries` read port with **no scope parameter to omit** — T-07.6.1.1 asks for a fitness test that the parameter cannot be skipped; there isn't one, the adapter resolves scope from the policy, and `ScopingRulesTest` already fails the build on a bypass · scope predicate appended **before** any caller filter, so a filter narrows and can never widen (AC-2) · `MyVisitorRequests` **refuses an unrecognised status** rather than dropping the filter — `?status=aproved` returning everything reads as "nothing decided yet" · detail is **one scoped query**, so foreign and missing ids share a code path with no ownership branch to time (AC-4) · approver exposed by **display name only** · `GET /api/v1/visitor-requests` and `/{id}` under `visitor.request`. **First story where a restricted principal reads over HTTP — Milestone F exercised end to end** | 9 `MyVisitorRequestsTest` (every status by db-value/name/case, five bogus values incl. an injection string, blank means no filter, clamp, dates) + TenantRequestsIT (13): scoped list both directions, status filter narrows-not-widens across two tenants, date range, unknown status 400, clamp, **rejection reason + timestamp + display name and no approver id/email/username**, markup transported as JSON data, **foreign id 404 byte-identical to a missing one**, tenant→queue 403 + audited denial, **tenant user with no tenant sees nothing**, 401 | *(local)* | pending | 0.2.0 |
| **US-07.3.1** | **FR-VMS-02 (SRS B1)** | `ApprovalQueueStore` read port kept **separate from the aggregate repository** — the projection is the security control, and visitor email/phone/document-ref are never selected rather than selected and dropped · `PageRequest` with the clamp in its **compact constructor**, so an unbounded page cannot be constructed at all (AC-5) · `JdbcApprovalQueueStore` ordering `created_at DESC, id DESC` — a tiebreak is what makes paging sound · **AC-6 answered by making `ScopePolicy` posture configuration** (`vms.scoping.posture`), logged at startup, rather than adding the second `ApprovalScopeStrategy` port T-07.3.1.2 names — two homes for isolation is what ADR-0005 exists to prevent · index already shipped in `V10`, so no migration · **`VisitorScopeSql.Clause.and()` closes the text-block `ANDTRUE` trap for good** after it recurred within a fortnight | 6 `PageRequestTest` (clamp at 10 000, boundary, nonsense size, long-arithmetic offset) + 3 `ScopePolicyTest` posture cases + ApprovalQueueIT (8): projection incl. counts and names, **no visitor PII in the response**, stable paging with no row seen twice or missed, 10 000→clamped with applied size returned, decided requests leave the queue, tenant 403 + audited denial, 401, **restrictive posture swapped in against the real database filters the result**, unscoped approver fails closed | *(local)* | pending | 0.2.0 |
| **US-07.4.2** | **FR-VMS-02 (SRS B1)** | `RejectVisitorRequest` mirroring approval (same compare-and-set, same one-transaction outbox write) · the mandatory reason stays an **aggregate invariant**, not a controller check, so a future bulk-rejection path cannot forget it · **`JsonText` — a correct JSON string escaper**, because AC-3 puts operator free text into a `jsonb` audit payload built by concatenation and the six existing per-entity `escape` helpers handle only `\` and `"`; a reason typed with a newline would have failed the insert · reason **stored verbatim, not sanitised** (AC-6) — encoding belongs at render · `POST /api/v1/visitor-requests/{id}/reject` under `visitor.approve` | 11 domain (`RejectionRulesTest`: every source state, blank/whitespace/null reason, trimmed, bounded, elapsed window still rejectable, markup verbatim) + 6 `JsonTextTest` + 12 use-case + VisitorRejectionIT (9): reject→row+visitors+event+audit-with-reason, **one event and zero credential rows (AC-4)**, six blank-reason bodies all 400 by direct API call, over-long 400, **hostile reason round-trips through `jsonb` intact**, approved→409, second reject 409, tenant 403 + denial audited, 404/401 | *(local)* | pending | 0.2.0 |
| **US-07.4.1** | **FR-VMS-02 (SRS B1)** | `V10__visitor_request_decisions.sql` (`decision_reason`, `decided_at`, CHECK that a reason implies a decision) · `approve(approver, note, now)` with the elapsed-window rule and time as a **parameter, not a clock read** · `ApproveVisitorRequest` — **compare-and-set on the status itself as the optimistic lock**, with the outbox write inside the same transaction so one winner means one event · `POST /api/v1/visitor-requests/{id}/approve` guarded by `visitor.approve` overriding the class-level `visitor.request`. **Fixed a latent US-03.4.1 defect**: `findById` built `WHERE id = ? ANDTRUE` because a Java text block strips trailing whitespace — the method had no HTTP route until now, so nothing had ever run it | 11 domain (`ApprovalRulesTest`: every source state, note optional/trimmed/bounded, elapsed-window **boundary at the closing instant**, in-progress window still approvable, mixed visitor set) + 9 use-case (single-event guarantee, one transaction, nothing written on any refusal) + VisitorApprovalIT (9): approve→row+visitors+event+audit, **two threads → one 200, one 409, exactly one outbox row and one audit row**, second approval 409, elapsed window 422, over-long note 400, tenant 403 + denial audited, 404, 401 | *(local)* | pending | 0.2.0 |
| **US-07.1.1** | **FR-VMS-01 (SRS B1)** ⭐ *first visitor requirement* | V7 grant + outbox · `VisitorRequest` aggregate + `Visitor`/`TimeWindow` (framework-free domain) · `SubmitVisitorRequest` · JDBC adapters · `POST /api/v1/visitor-requests` | 8 domain + 8 use-case unit + VisitorRequestIT (6): submit→row+visitor+event+audit, invalid window 400, tenant override ignored, foreign host, 403 w/o permission, 401 | *(local)* | pending | 0.2.0 |

---

## Coverage dashboard

| Metric | Count | % |
|---|---|---|
| SRS FRs traced to an epic | 28 / 28 | **100%** |
| SRS FRs traced to a feature | 28 / 28 | **100%** |
| SRS FRs decomposed to user stories | 28 / 28 | **100%** |
| SRS FRs decomposed to tasks | 28 / 28 | **100%** |
| SRS FRs fully ready | 4 / 28 | 14% |
| SRS FRs sim-only (build now, verify after TODO-02) | 1 / 28 | 4% |
| SRS FRs partially workable | 18 / 28 | 64% |
| SRS FRs blocked outright | 5 / 28 | 18% |
| SRS FRs implemented | 0 / 28 | 0% |
| NFRs with a testable acceptance criterion | 5 / 8 | 63% |
| User stories authorized | 105 / 178 | 59% |
| Open discrepancies | 15 | — |
| Open questions | 19 (4 blocking) | — |

**Read this honestly.** Requirement *coverage* is complete — every SRS requirement is decomposed to
task level and nothing has been missed. Requirement *readiness* is not. Only **4 of 28 are fully ready**; **18 are partially workable** —
their core behaviour can be built while a dependent slice waits — and **5 are blocked outright**.

The dominant cause is TODO-01, not TODO-02. The missing SRS v2 touches almost every requirement,
because master data (EPIC-04) is unbacked and nearly everything references a tenant, floor or
reception. TODO-02 blocks less than it appears to: the anti-corruption layer (ADR-0002) converts it
from a build blocker into a *verification* blocker for most credential work.

That is a supplier and client dependency, not a planning gap — and the phase ordering is built
around it, front-loading everything that can proceed without them.
